struct StructName 
{
  int property;
 } *variableName;
 
 //inicializando ponteiro
 variableName = (struct variableName*) malloc (sizeof (struct StructName));
 //(struct variableName*) type cast
 // malloc (sizeof (struct StructName)) reserva a memória e retorna um ponteiro
 
 //acessando property
 variableName->property;
